import * as React from "react"
import Layout from "../components/layout";

const AboutPage = () => {
    return (
        <Layout pageTitle="About Bear's Cup of Coffee">
        </Layout>
    )
}

export const Head = () => <title>About Us</title>

export default AboutPage;
