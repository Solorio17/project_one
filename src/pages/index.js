import * as React from "react"
import Layout from "../components/layout";

const HomePage = () => {
  return (
    <Layout pageTitle="Bear's Cup of Coffee">
    </Layout>
  )
}

export const Head = () => <title>Home Page</title>

export default HomePage;
